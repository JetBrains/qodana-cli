{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:base"
  ],
  "ignorePaths": [
    "**/node_modules/**",
    "**/bower_components/**",
    "**/vendor/**",
    "**/examples/**",
    "**/__tests__/**",
    "**/test/**",
    "**/tests/**",
    "**/__fixtures__/**",
    "**/testdata/**",
    ".teamcity/**"
  ],
  "regexManagers": [
    {
      "fileMatch": [
        "(^|/)(.*\\.)?Dockerfile$"
      ],
      "matchStrings": [
        "#\\s*renovate:\\s*datasource=(?<datasource>.*?) depName=(?<depName>.*?)( versioning=(?<versioning>.*?))?\\sENV .*?_VERSION=\"?(?<currentValue>.*?)\"?\\s"
      ],
      "versioningTemplate": "{{#if versioning}}{{versioning}}{{else}}semver{{/if}}"
    },
    {
      "fileMatch": [
        "internal/tooling/scripts/build-qodana-jbr\\.go$"
      ],
      "matchStrings": [
        "renovate:\\s*datasource=(?<datasource>.*?) depName=(?<depName>.*?)\\n.*?\"(?<currentValue>.*?)\""
      ],
      "versioningTemplate": "regex:^jbr-release-(?<major>\\d+)\\.(?<minor>\\d+)\\.(?<patch>\\d+)b(?<build>.*)$"
    }
  ],
  "packageRules": [
    {
      "description": "Qodana artifacts aren't on Maven Central; querying it anyway risks a 429 that aborts the whole run.",
      "matchDatasources": ["maven"],
      "matchPackageNames": ["org.jetbrains.qodana:*", "com.jetbrains.qodana:*"],
      "registryUrls": ["https://packages.jetbrains.team/maven/p/ij/intellij-dependencies"]
    },
    {
      "datasources": ["repology"],
      "packagePatterns": [
        "^debian_11/"
      ],
      "separateMajorMinor": false,
      "groupName": "debian packages",
      "groupSlug": "debian",
      "paths": ["dockerfiles/**"]
    },
    {
      "datasources": ["docker"],
      "paths": ["dockerfiles/**"]
    }
  ],
  "commitMessagePrefix": "QD-8148 ",
  "schedule": ["* 0-3 1 * *"]
}
