name: Setup Podman
description: Install Podman (v4.9.4 or newer) and point the Docker CLI at it
inputs:
  version:
    required: true
    default: latest
    description: Either "latest" or a full "[v]major.minor.patch" version.
runs:
  using: composite
  steps:
    - name: Set up Docker APT repository
      shell: bash
      run: | # shell
        set -euo pipefail

        # See https://docs.docker.com/engine/install/ubuntu/#install-using-the-repository
        sudo apt-get update
        sudo apt-get install ca-certificates curl
        sudo install -m 0755 -d /etc/apt/keyrings
        sudo curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
        sudo chmod a+r /etc/apt/keyrings/docker.asc

        # Add the repository to Apt sources:
        echo \
          "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/ubuntu \
          $(. /etc/os-release && echo "${UBUNTU_CODENAME:-$VERSION_CODENAME}") stable" | \
          sudo tee /etc/apt/sources.list.d/docker.list > /dev/null
        sudo apt-get update

    - name: Install Docker CLI
      shell: bash
      run: sudo apt-get install docker-ce-cli

    - name: Install Podman Engine
      shell: bash
      # Passed through the environment rather than interpolated into the script.
      env:
        VERSION: ${{ inputs.version }}
      run: ${{ github.action_path }}/install-podman.sh

    - name: Start Podman Engine
      shell: bash
      run: ${{ github.action_path }}/start-podman.sh
